@if (User.Identity.IsAuthenticated && (User.IsInRole(OwnerRoleName) || User.IsInRole(ManagerRoleName)))
{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
else
{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
